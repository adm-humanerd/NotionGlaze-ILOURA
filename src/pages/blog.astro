---
import Layout from '../layouts/Layout.astro';
import { fetchStrapi } from "../utils/strapi";

// Strapi에서 글로벌 설정(색상 등)을 가져와야 'Insight Archive' 색상도 맞출 수 있습니다.
// 하지만 CSS 변수(--color-primary)는 Layout에서 이미 선언했으므로, 
// 여기서는 class 이름만 'text-primary'로 쓰면 자동으로 적용됩니다!

const posts = await fetchStrapi('post-pages?populate=*&sort=publishedAt:desc');
---

<Layout title="Home">
  <main class="max-w-6xl mx-auto px-6 py-12 md:py-20">
    
    {/* 히어로 섹션 */}
    <div class="text-center mb-20 space-y-4">
      <h1 class="text-5xl md:text-7xl font-black text-slate-900 tracking-tight">
        Insight 
        {/* 👇 [수정] text-pink-500 -> text-primary */}
        <span class="text-primary">Archive</span>
      </h1>
      <p class="text-xl text-slate-500 max-w-2xl mx-auto font-medium">
        Notion에서 작성하고 자동으로 발행되는 블로그입니다.
      </p>
    </div>

    {/* 게시글 그리드 */}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {posts && posts.map((post: any) => (
        <article class="group bg-white rounded-3xl overflow-hidden border border-slate-100 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 flex flex-col h-full">
          
          {/* 이미지 영역 */}
          <a href={`/post/${post.Slug}`} class="block overflow-hidden aspect-[1.5]">
             {/* ... 이미지가 있으면 표시 ... */}
             <div class="w-full h-full bg-slate-50 flex items-center justify-center group-hover:scale-105 transition-transform duration-500">
                <span class="text-4xl">📝</span>
             </div>
          </a>

          <div class="p-6 flex flex-col flex-1">
            <div class="flex items-center gap-3 mb-3">
               {/* 👇 [수정] 날짜/태그 색상도 primary로 변경 */}
               <span class="text-xs font-bold text-primary bg-slate-50 px-2 py-1 rounded">
                 {new Date(post.publishedAt).toLocaleDateString()}
               </span>
            </div>
            
            <h2 class="text-xl font-bold text-slate-900 mb-3 leading-snug group-hover:text-primary transition-colors">
              <a href={`/post/${post.Slug}`}>
                {post.Title}
              </a>
            </h2>

            <p class="text-slate-500 text-sm line-clamp-2 mb-4 flex-1">
              {/* 요약문이 있다면 표시 */}
              클릭하여 내용을 확인하세요.
            </p>
            
            <a href={`/post/${post.Slug}`} class="inline-flex items-center text-sm font-bold text-slate-900 hover:text-primary transition-colors mt-auto">
              Read Article 
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M12.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
            </a>
          </div>
        </article>
      ))}
    </div>

  </main>
</Layout>
